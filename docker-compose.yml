version: '3.8'

services:
  admire_trello:
    restart: unless-stopped
    container_name: fur_chins
    build: .
    stdin_open: true
    tty: true

networks:
  default:
    external:
      name: server
